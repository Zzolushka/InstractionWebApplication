@model List<InstructionsWebApplication.Models.Page>
.......................................
@using (Html.BeginForm())
{
    for(int i=0; i<Model.Count; i++)
    {
        <h4>Страница № @(i+1)</h4>
         
        @: Text: @Html.EditorFor(m=>m[i].Text)
        @: ImageURL: @Html.EditorFor(m => m[i].ImageURL)
    }
    <input type="submit" />

}